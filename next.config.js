/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.pexels.com', '4.bp.blogspot.com', 'images.unsplash.com'],
  },
};

module.exports = nextConfig;
